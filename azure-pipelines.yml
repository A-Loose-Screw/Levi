trigger:
- master

jobs:
- job: linux
  pool:
    vmImage: 'ubuntu-latest'
  steps:
    - template: 'azure-steps.yml'

jobs:
- job: mac
  pool:
    vmImage: 'macOS-latest '
  steps:
    - template: 'azure-steps.yml'

jobs:
- job: windows
  pool:
    vmImage: 'windows-latest'
  steps:
    - template: 'azure-steps.yml'